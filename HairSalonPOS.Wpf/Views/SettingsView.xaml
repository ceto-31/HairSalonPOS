<UserControl x:Class="Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="500" d:DesignWidth="700">
    <Border Style="{StaticResource CardBorder}" MaxWidth="640" HorizontalAlignment="Left">
        <StackPanel>
            <TextBlock Text="Printer &amp; Receipt Settings" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,16"/>
            <TextBlock Text="Salon details (printed on every receipt)" Style="{StaticResource SectionHeader}"/>
            <TextBox Text="{Binding SalonName}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,8"/>
            <TextBox Text="{Binding SalonAddress}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,8"/>
            <TextBox Text="{Binding SalonTelephone}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,8"/>
            <TextBox Text="{Binding SalonTin}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16"/>

            <TextBlock Text="Printer type" Style="{StaticResource SectionHeader}"/>
            <ComboBox ItemsSource="{Binding PrinterTypes}" SelectedItem="{Binding PrinterType}"
                      Style="{StaticResource ModernComboBox}" Margin="0,0,0,8"/>
            <TextBlock Text="Standard: A4/laser via FlowDocument + PrintDialog. Thermal: 80mm roll via ESC/POS raw commands."
                       Foreground="{DynamicResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,0,0,12"/>

            <StackPanel Visibility="{Binding IsThermalSelected, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="Thermal printer name (exact Windows printer name)" Style="{StaticResource SectionHeader}"/>
                <TextBox Text="{Binding ThermalPrinterName}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,12"/>
            </StackPanel>

            <Button Content="Save settings" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" HorizontalAlignment="Left"/>
            <TextBlock Text="{Binding StatusMessage}" Margin="0,12,0,0" Foreground="{DynamicResource AccentBrush}" TextWrapping="Wrap"/>
        </StackPanel>
    </Border>
</UserControl>
